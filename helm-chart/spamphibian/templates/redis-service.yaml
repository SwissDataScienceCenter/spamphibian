# {{- if not .Values.global.redis.useExternalRedis -}}
# apiVersion: v1
# kind: Service
# metadata:
#   name: {{ include "spamphibian.fullname" . }}-redis
# spec:
#   selector:
#     app: {{ include "spamphibian.name" . }}-redis
#   ports:
#     - protocol: TCP
#       port: {{ .Values.global.gitlab.accessToken }}
#       targetPort: 6379
# {{- end }}
